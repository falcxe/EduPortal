name: Ping Service
on:
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping
      run: curl https://eduportal-d2gf.onrender.com/